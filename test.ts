// tests go here; this will not be compiled when this package is used as an extension.
basic.forever(() => { 

    while (true)
    {
        //ZDR.ReadPin(0);
        console.log("abcdefg");
    }    
  
}
)